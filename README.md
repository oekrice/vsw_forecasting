Initial repository for the Solar Wind forecasting code, using Outflow fields as a base model.

Installation:

Currently needs python 3.12 as 3.14 is too new for various of the dependencies
```
python3.12 -m venv .venv
source .venv/bin/activate.csh
pip install -e .
```


